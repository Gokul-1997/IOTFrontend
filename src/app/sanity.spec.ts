/*
 * Placeholder so the unit-test runner has at least one spec to discover.
 * Real component tests should be added next to each component file.
 * End-to-end coverage lives in /e2e/ (Playwright).
 */

describe('sanity', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
